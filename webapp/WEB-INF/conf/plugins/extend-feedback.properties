################################################################################
# Configuration file for Extend plugin

# Set true if a captcha must be used, false otherwise
# Note : The plugin-jcaptcha must be installed in the webapp
module.extend.feedback.useCaptcha=true

module.extend.feedback.titleKey=module.extend.feedback.extender.feedback.label
